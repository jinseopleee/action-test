{
  "name": "@musinsa/mads-design-token-generator-action",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm build && node dist/index.js",
    "dev:watch": "concurrently -n BUILD,RUN -c blue,green \"=pnpm build --watch\" \"node --watch dist/index.js\"",
    "build": "rm -rf dist && pnpm tsc && vite build",
    "tsc": "tsc"
  },
  "devDependencies": {
    "@actions/core": "^1.11.1",
    "@figma/rest-api-spec": "^0.33.0",
    "@types/node": "^20.19.9",
    "concurrently": "^9.2.0",
    "typescript": "5.5.4",
    "vite": "^7.0.6"
  },
  "dependencies": {
    "axios": "^1.11.0"
  }
}
